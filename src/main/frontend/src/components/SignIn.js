import React from 'react';

const SignIn = () => {
    return (
        <div>
            <h1>@ 멤버
                mem_id(pk)
                이메일 아이디(unique)
                닉네임(unique)
                생년월일
                비밀번호</h1>
        </div>
    );
};

export default SignIn;
